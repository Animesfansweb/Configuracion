server:
  port: 8073

# CONECCION A BASE DE DATOS
spring:
  datasource:
    url: jdbc:postgresql://10.35.2.184:5431/GOTALENT
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
# Hibernate ddl auto (create, create-drop, validate, update)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      jdbc:
        lob:
          non_contextual_creation: true

